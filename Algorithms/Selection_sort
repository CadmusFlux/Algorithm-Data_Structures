Code : 
        def SelectSort(list):
          for i in range(len(list)):
            min = i
            for j in range(i+1,len(list))
              if list[j]<list[min]:
                min = j
                list[i],list[j] = list[j],list[i]
          return list

        SelectSort([29,45,8,11,6,1])
